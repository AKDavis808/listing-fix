# Copy to `.env` at the project root (same folder as `shopify.app.toml`) for local development.
# `shopify app dev` merges CLI-provided Shopify variables with `.env`.

OPENAI_API_KEY=your_openai_api_key_here

# Optional — defaults to gpt-4o-mini if unset.
# OPENAI_MODEL=gpt-4o-mini
